<%= form_with(model: external_event, local: true) do |form| %>
  <% if external_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(external_event.errors.count, "error") %> prohibited this external_event from being saved:</h2>

      <ul>
      <% external_event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :external_event_name %>
  </div>

  <div class="field">
    <%= form.label :start %>
    <%= form.date_select :start, id: :external_event_start %>
  </div>

  <div class="field">
    <%= form.label :end %>
    <%= form.date_select :end, id: :external_event_end %>
  </div>
  <br>
  <div class="actions">
    <%= form.submit 'Submit', class:"btn btn-success"%>
  </div>
<% end %>
